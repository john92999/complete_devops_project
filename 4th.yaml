apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: nginx
spec:
  selector:
    matchLabels:
      app: nginx
  ports:
    - protocol: TCP
      targetPort: 80
      port: 8095
  type: ClusterIP
