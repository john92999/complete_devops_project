apiVersion: v1
kind: Service
metadata:
  name: mongo-service
  namespace: mongo
spec:
  selector:
    app: mongo-db
  ports:
    - port: 27017
      protocol: TCP
      targetPort: 27017
      nodePort: 30017
  type: NodePort
