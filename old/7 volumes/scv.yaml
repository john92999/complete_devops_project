apiVersion: v1
kind: Service
metadata:
  name: mongo-svc
spec:
  selector:
    app: mongo
  ports:
    - protocol: TCP
      targetPort: 27017
      port: 27017
      nodePort: 32000
  type: NodePort
